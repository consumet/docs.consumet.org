import { Tab, Tabs } from "nextra-theme-docs";

# Get Started

The "consumet.ts" library, otherwise @consumet/extensions, is available via mainstream package managers.

## Installation

Instructions for installing the library can be found below.

{/* NOTE: do NOT run a formatter for the time being; it breaks the code blocks below. */}

<Tabs items={["npm", "yarn"]}>
  <Tab>
  ```bash
  npm i @consumet/extensions
  ```
  View package at [npmjs.com](https://www.npmjs.com/package/@consumet/extensions)
  </Tab>
  <Tab>
  ```bash
  yarn add @consumet/extensions
  ```
  View package at [yarnpkg.com](https://yarnpkg.com/package/@consumet/extensions)
  </Tab>
</Tabs>
